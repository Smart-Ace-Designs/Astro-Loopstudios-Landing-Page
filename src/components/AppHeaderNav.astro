---
import navLinks from "@/data/navitems.json";
---

<nav class="text-theme-white font-alata hidden text-sm md:block">
  <ul class="flex flex-row gap-10">
    {
      navLinks.map((link) => (
        <li>
          <a href={link.href}>{link.label}</a>
        </li>
      ))
    }
  </ul>
</nav>
